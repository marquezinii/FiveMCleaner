<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Ralven.App.Controls"
                    xmlns:services="clr-namespace:Ralven.App.Services">
    <services:LocalizedStrings x:Key="DialogStrings" />
    <Style x:Key="DialogWindowStyle" TargetType="{x:Type controls:DialogWindow}">
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="ShowInTaskbar" Value="False" />
        <Setter Property="Background" Value="{DynamicResource Surface1Brush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Cycle" />
        <Setter Property="KeyboardNavigation.ControlTabNavigation" Value="Cycle" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:DialogWindow}">
                    <AdornerDecorator>
                        <Border x:Name="PART_Surface" Background="{TemplateBinding Background}" BorderThickness="0" Padding="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="56" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid x:Name="PART_Header" Background="Transparent" Margin="24,0,12,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBlock Text="{TemplateBinding Title}" Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="13" FontWeight="Medium" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                    <Button x:Name="DismissButton" Grid.Column="1" Width="36" Height="36"
                                            Style="{DynamicResource IconButtonStyle}"
                                            Command="{x:Static SystemCommands.CloseWindowCommand}"
                                            CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                            AutomationProperties.Name="{Binding [Dialog.Close], Source={StaticResource DialogStrings}}"
                                            ToolTip="{Binding [Dialog.Close], Source={StaticResource DialogStrings}}">
                                        <Path Data="M 2,2 L 12,12 M 12,2 L 2,12" Stroke="{DynamicResource TextSecondaryBrush}"
                                              StrokeThickness="1.5" Width="14" Height="14" />
                                    </Button>
                                </Grid>
                                <ContentPresenter Grid.Row="1" />
                            </Grid>
                        </Border>
                    </AdornerDecorator>
                    <ControlTemplate.Triggers>
                        <Trigger Property="CanDismiss" Value="False">
                            <Setter TargetName="DismissButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="DialogFooterStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Surface1Brush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="32,16,32,24" />
    </Style>
</ResourceDictionary>

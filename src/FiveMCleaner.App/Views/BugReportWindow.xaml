<Window x:Class="FiveMCleaner.App.Views.BugReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{Binding [BugReport.WindowTitle], Source={StaticResource LocalizedStrings}}"
        Icon="/FiveMCleaner;component/Assets/FiveMCleaner.png"
        Width="680"
        Height="660"
        MinWidth="560"
        MinHeight="440"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{DynamicResource BackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="7" CornerRadius="0" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource StrongBorderBrush}" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="54" />
                <RowDefinition Height="*" />
                <RowDefinition Height="70" />
            </Grid.RowDefinitions>

            <Border Background="{DynamicResource ChromeBrush}" BorderBrush="{DynamicResource BorderSoftBrush}" BorderThickness="0,0,0,1" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="18,0,8,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="30" Height="30" CornerRadius="8" Background="#20FF7A18">
                            <TextBlock Text="&#xE74C;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource OrangeLightBrush}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding [BugReport.Link], Source={StaticResource LocalizedStrings}}" FontSize="14" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding [BugReport.FormLanguage], Source={StaticResource LocalizedStrings}}" FontSize="9.5" Foreground="{DynamicResource TextSubtleBrush}" />
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Style="{StaticResource WindowButtonStyle}" Click="Close_Click" ToolTip="{Binding [Window.Close], Source={StaticResource LocalizedStrings}}">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" />
                    </Button>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="28,24,28,20">
                    <TextBlock Text="{Binding [BugReport.Heading], Source={StaticResource LocalizedStrings}}" FontSize="22" FontWeight="Bold" />
                    <TextBlock Text="{Binding [BugReport.Intro], Source={StaticResource LocalizedStrings}}" Margin="0,6,0,18" Foreground="{DynamicResource TextMutedBrush}" FontSize="11.5" TextWrapping="Wrap" />

                    <Border Background="#1537C889" BorderBrush="#4037C889" BorderThickness="1" CornerRadius="9" Padding="13" Margin="0,0,0,18">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="28" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <TextBlock Text="&#xEA18;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource GreenBrush}" FontSize="14" VerticalAlignment="Top" />
                            <TextBlock Grid.Column="1" Text="{Binding [BugReport.Privacy], Source={StaticResource LocalizedStrings}}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10.5" TextWrapping="Wrap" />
                        </Grid>
                    </Border>

                    <TextBlock Text="{Binding [BugReport.Category.Label], Source={StaticResource LocalizedStrings}}" FontSize="11.5" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <WrapPanel x:Name="CategoryPanel" Margin="0,0,0,9">
                        <RadioButton GroupName="BugCategory" Style="{StaticResource ChoiceRadioStyle}" Tag="{Binding [BugReport.Category.Optimization], Source={StaticResource LocalizedStrings}}" Content="{Binding [BugReport.Category.Optimization], Source={StaticResource LocalizedStrings}}" Checked="Category_Checked" />
                        <RadioButton GroupName="BugCategory" Style="{StaticResource ChoiceRadioStyle}" Tag="{Binding [BugReport.Category.Games], Source={StaticResource LocalizedStrings}}" Content="{Binding [BugReport.Category.Games], Source={StaticResource LocalizedStrings}}" Checked="Category_Checked" />
                        <RadioButton GroupName="BugCategory" Style="{StaticResource ChoiceRadioStyle}" Tag="{Binding [BugReport.Category.Windows], Source={StaticResource LocalizedStrings}}" Content="{Binding [BugReport.Category.Windows], Source={StaticResource LocalizedStrings}}" Checked="Category_Checked" />
                        <RadioButton GroupName="BugCategory" Style="{StaticResource ChoiceRadioStyle}" Tag="{Binding [BugReport.Category.Interface], Source={StaticResource LocalizedStrings}}" Content="{Binding [BugReport.Category.Interface], Source={StaticResource LocalizedStrings}}" Checked="Category_Checked" />
                        <RadioButton GroupName="BugCategory" Style="{StaticResource ChoiceRadioStyle}" Tag="{Binding [BugReport.Category.Crash], Source={StaticResource LocalizedStrings}}" Content="{Binding [BugReport.Category.Crash], Source={StaticResource LocalizedStrings}}" Checked="Category_Checked" />
                        <RadioButton GroupName="BugCategory" Style="{StaticResource ChoiceRadioStyle}" Tag="{Binding [BugReport.Category.Other], Source={StaticResource LocalizedStrings}}" Content="{Binding [BugReport.Category.Other], Source={StaticResource LocalizedStrings}}" Checked="Category_Checked" />
                    </WrapPanel>

                    <TextBlock Text="{Binding [BugReport.Summary.Label], Source={StaticResource LocalizedStrings}}" FontSize="11.5" FontWeight="SemiBold" Margin="0,0,0,7" />
                    <TextBox x:Name="SummaryTextBox" Style="{StaticResource FormTextBoxStyle}" MaxLength="120" ToolTip="{Binding [BugReport.Summary.Tooltip], Source={StaticResource LocalizedStrings}}" />
                    <TextBlock Text="{Binding [BugReport.Summary.Example], Source={StaticResource LocalizedStrings}}" Margin="2,5,0,15" Foreground="{DynamicResource TextSubtleBrush}" FontSize="9.5" TextWrapping="Wrap" />

                    <TextBlock Text="{Binding [BugReport.Description.Label], Source={StaticResource LocalizedStrings}}" FontSize="11.5" FontWeight="SemiBold" Margin="0,0,0,7" />
                    <TextBox x:Name="DescriptionTextBox" Style="{StaticResource FormTextBoxStyle}" MinHeight="128" MaxHeight="210" MaxLength="8000" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto" ToolTip="{Binding [BugReport.Description.Tooltip], Source={StaticResource LocalizedStrings}}" />
                    <TextBlock Text="{Binding [BugReport.Description.Help], Source={StaticResource LocalizedStrings}}" Margin="2,5,0,15" Foreground="{DynamicResource TextSubtleBrush}" FontSize="9.5" TextWrapping="Wrap" />

                    <TextBlock Text="{Binding [BugReport.Log.Label], Source={StaticResource LocalizedStrings}}" FontSize="11.5" FontWeight="SemiBold" Margin="0,0,0,7" />
                    <TextBox x:Name="LogTextBox" Style="{StaticResource FormTextBoxStyle}" MinHeight="80" MaxHeight="150" MaxLength="100000" AcceptsReturn="True" TextWrapping="NoWrap" VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" ToolTip="{Binding [BugReport.Log.Tooltip], Source={StaticResource LocalizedStrings}}" />
                    <TextBlock Text="{Binding [BugReport.Log.Help], Source={StaticResource LocalizedStrings}}" Margin="2,5,0,15" Foreground="{DynamicResource TextSubtleBrush}" FontSize="9.5" TextWrapping="Wrap" />

                    <TextBlock Text="{Binding [BugReport.Email.Label], Source={StaticResource LocalizedStrings}}" FontSize="11.5" FontWeight="SemiBold" Margin="0,0,0,7" />
                    <TextBox x:Name="EmailTextBox" Style="{StaticResource FormTextBoxStyle}" MaxLength="254" ToolTip="{Binding [BugReport.Email.Tooltip], Source={StaticResource LocalizedStrings}}" />
                    <TextBlock Text="{Binding [BugReport.Email.Help], Source={StaticResource LocalizedStrings}}" Margin="2,5,0,15" Foreground="{DynamicResource TextSubtleBrush}" FontSize="9.5" TextWrapping="Wrap" />

                    <Border Style="{StaticResource CardStyle}" Padding="14" Margin="0,0,0,13">
                        <CheckBox x:Name="IncludeTechnicalInfoCheckBox" Style="{StaticResource ToggleSwitchStyle}" IsChecked="True">
                            <StackPanel>
                                <TextBlock Text="{Binding [BugReport.Technical.Title], Source={StaticResource LocalizedStrings}}" FontSize="11.5" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding [BugReport.Technical.Help], Source={StaticResource LocalizedStrings}}" Margin="0,3,0,0" Foreground="{DynamicResource TextSubtleBrush}" FontSize="9.5" TextWrapping="Wrap" />
                            </StackPanel>
                        </CheckBox>
                    </Border>

                    <Border x:Name="StatusBorder" Background="{DynamicResource InsetBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="11" Visibility="Collapsed">
                        <TextBlock x:Name="StatusText" Foreground="{DynamicResource TextMutedBrush}" FontSize="10.5" TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="{DynamicResource ChromeBrush}" BorderBrush="{DynamicResource BorderSoftBrush}" BorderThickness="0,1,0,0" CornerRadius="0,0,12,12">
                <Grid Margin="20,0">
                    <Button x:Name="CopyButton" Content="{Binding [BugReport.Copy], Source={StaticResource LocalizedStrings}}" Style="{StaticResource LinkButtonStyle}" HorizontalAlignment="Left" Click="Copy_Click" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="{Binding [BugReport.Cancel], Source={StaticResource LocalizedStrings}}" Style="{StaticResource SecondaryButtonStyle}" Height="40" Padding="17,0" Margin="0,0,9,0" Click="Close_Click" />
                        <Button x:Name="SendButton" Content="{Binding [BugReport.Send], Source={StaticResource LocalizedStrings}}" Style="{StaticResource PrimaryButtonStyle}" Height="40" Padding="20,0" Click="Send_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
